"""
cad_common.py

Common utilities and constants for the CAD framework.
This module provides shared types, constants, and logging configuration used across the codebase.
"""

import logging

# Configure logging for the CAD framework
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

# TYPES

# CONSTANTS
